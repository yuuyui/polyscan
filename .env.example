# Polyscan Environment Variables
# Copy to .env.local and set values for production

# CLOB API (default: /api — uses Vite proxy in dev)
VITE_CLOB_HOST=https://clob.polymarket.com

# Gamma API (default: /gamma — uses Vite proxy in dev)
VITE_GAMMA_HOST=https://gamma-api.polymarket.com
